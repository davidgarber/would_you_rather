$("#new_question").remove();
$("#new_link").show();
$("#questions").append('<%= j render(@question) %>');
<% if @question %>
  alert("Success!");
<% else %>
  $('#errors').html("<%= j(render 'layouts/errors', :object => @question) %>");
<% end %>
